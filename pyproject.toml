[project]
name = "ai-presentation-coach-site"
version = "0.1.0"
description = "AI presentation coach web service"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=1.0.0"
]

[tool.vercel]
entrypoint = "api.analyze:handler"
